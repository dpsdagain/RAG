# ──────────────────────────────────────────────
# RAG System – Python dependencies
# ──────────────────────────────────────────────

# ── Core web framework & settings ──
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
pydantic>=2.0
pydantic-settings>=2.3.0
pyyaml>=6.0.1
httpx>=0.27.0
aiofiles>=24.1.0

# ── Database ──
sqlite-vec>=0.1.1

# ── Embeddings & ML inference ──
onnxruntime>=1.18.0
tokenizers>=0.19.0
numpy>=1.26.0
huggingface-hub>=0.23.0

# ── Cloud embeddings (Voyage AI) ──
# voyage-code-3 is the default embedder (provider=voyage in config.yaml) —
# tuned for code + documentation. Needs RAG_EMBEDDING__API_KEY in .env.
voyageai>=0.3.0

# ── Reranking ──
# sentence-transformers is the primary backend (modern HF cross-encoders
# like BAAI/bge-reranker-v2-m3). flashrank is kept as a lightweight
# fallback for the legacy ms-marco family.
sentence-transformers>=3.0.0
flashrank>=0.2.0

# ── Structured logging & observability ──
structlog>=24.2.0
psutil>=5.9.0

# ── Document parsing – PDF ──
pymupdf4llm>=0.0.5

# ── Document parsing – code tree-sitter grammars ──
tree-sitter>=0.22.0
tree-sitter-python>=0.21.0
tree-sitter-javascript>=0.21.0
tree-sitter-typescript>=0.21.0
tree-sitter-java>=0.21.0
tree-sitter-go>=0.21.0
tree-sitter-rust>=0.21.0
tree-sitter-c>=0.21.0
tree-sitter-cpp>=0.22.0

# ── Optional: advanced document parsing ──
# docling>=1.0.0
# paddleocr>=2.7.0
# paddlepaddle>=2.6.0

# ── Optional: LlamaParse cloud parsing ──
# llama-parse>=0.4.0

# ── Optional: web crawling ──
# crawl4ai>=0.3.0

# ── Dev / testing ──
pytest>=8.2.0
pytest-asyncio>=0.23.0

# ── Dev / evaluation ──
# ragas>=0.1.0

# ── Frontend UI ──
chainlit>=1.1.0
httpx-sse>=0.4.0
